import { FocusTriggeringEventEmitterDirective } from './focus-triggering-event-emitter.directive';

describe('FocusTriggeringEventEmitterDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusTriggeringEventEmitterDirective();
    expect(directive).toBeTruthy();
  });
});
